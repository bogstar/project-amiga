using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace Amiga.UI
{
	public class GUIManager : MonoBehaviour
	{/*
		[SerializeField]
		GameObject versionPrefab;
		[SerializeField]
		GameObject guiElements;
		[SerializeField]
		bool allowInventory;
		[SerializeField]
		bool allowMenu;
		[SerializeField]
		bool showVersion;

		Button menuButton;
		Button inventoryButton;

		void Start()
		{
			if(allowMenu || allowInventory)
			{

				GameObject go = GameObject.Instantiate(guiElements, transform);
				go.name = "GUI Elements";
				menuButton = go.transform.Find("Options Button").GetComponent<Button>();
				inventoryButton = go.transform.Find("Inventory Button").GetComponent<Button>();
				inventoryButton.interactable = allowInventory;
				
			}
			
			if (showVersion)
			{
				GameObject.Instantiate(versionPrefab, transform).name = versionPrefab.name;
			}
		}*/
	}
}